// Widget tests are added per feature in test/widget/features/.
// This file is intentionally left as a placeholder.

import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('placeholder — no-op', (tester) async {
    expect(true, isTrue);
  });
}
